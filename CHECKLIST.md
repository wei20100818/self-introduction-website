# 個人作品集網站 Checklist

> 最後依程式碼盤點：2026-08-02  
> 使用方式：`[x]` 代表已由本次程式碼／指令驗證；`[ ]` 代表仍需人工、外部服務或發布環境驗收。每次發布前請由上而下檢查。

## 1. 發布前必要驗證

- [x] 安裝依賴可重現：專案已有 `package-lock.json`，可使用 `npm ci`。
- [x] 靜態檢查通過：執行 `npm run lint` 無錯誤。
- [x] 自動測試通過：執行 `npm run test`，目前為 7 個測試檔、17 項測試全數通過。
- [x] 生產版可建置：執行 `npm run build` 成功，TypeScript 編譯與 Vite 打包均通過。
- [ ] 以 `npm run preview` 手動瀏覽正式建置版本，而不是只測開發伺服器。
- [ ] 使用乾淨環境執行一次 `npm ci && npm run lint && npm run test && npm run build`。
- [ ] 確認瀏覽器 Console 沒有錯誤、未處理的 Promise 或資源載入失敗。
- [ ] 確認 Network 沒有 404、混合內容（HTTP/HTTPS）或被 CORS 阻擋的請求。

## 2. 網站內容與品牌資料

- [ ] 確認 `src/data/profile.ts` 的姓名、主視覺標語與聯絡信箱正確且仍為最新資料。
- [ ] 確認個人照片 `public/images/profile/profile.png` 是已授權、適合公開的版本。
- [ ] 檢查首頁、自我介紹、作品、贊助、聯絡各區塊的中文文字無錯字、亂碼或過期資訊。
- [ ] 切換英文後，確認英文文案自然、標點與大小寫一致；作品名稱依目前設計維持中文時亦應可理解。
- [ ] 確認每個作品的標題、說明、封面圖與替代文字皆相互對應。
- [ ] 檢查外部作品連結：猜數字遊戲與 Chrome 線上應用程式商店連結可正常開啟。
- [ ] 確認外部網站或圖片沒有洩漏私人資訊、未公開的帳號或測試資料。
- [ ] 若履歷、社群或 GitHub 連結要加入網站，確認公開對象與顯示名稱正確。

## 3. 首頁與導覽功能

- [ ] 重新載入首頁後，首頁、關於我、作品、聯絡我等導覽項目可平滑抵達正確區段。
- [ ] 捲動頁面時，頁首的目前區段提示會正確更新。
- [ ] 行動版選單、桌面版選單與頁尾連結都可操作，且不會被內容遮擋。
- [ ] 鍵盤按 Tab 可依合理順序走訪所有互動元件；跳至主要內容連結可正常運作。
- [ ] 主題切換可在亮／暗模式間正確切換，並在重新整理後保留偏好設定。
- [ ] 語言切換可在繁中／英文間切換，並在重新整理後保留偏好設定。
- [ ] 切換語言時，頁面 `lang` 屬性與各表單／導覽文案一致。
- [ ] 系統偏好深色模式時，首次造訪的預設主題顯示正確，且不會產生明顯閃白。
- [ ] 開啟「減少動態效果」系統設定後，主要動態效果不造成干擾。

## 4. 作品與貪食蛇遊戲

- [ ] 三張作品封面在桌面與手機寬度下都維持清楚比例，沒有拉伸、裁切重點或破圖。
- [ ] 各作品卡的按鈕／連結可用鍵盤開啟，且外部連結行為符合預期。
- [x] 貪食蛇有單元測試覆蓋：初始食物避開蛇身、禁止立即反向、吃食物加分／成長、撞牆結束與能量核心逾期等規則。
- [ ] 從作品區進入 `#/snake` 後，遊戲畫面可載入且不殘留一般網站的頁首／頁尾。
- [ ] 在鍵盤與觸控裝置上都能控制蛇的移動；方向改變不會意外反向。
- [ ] 實測吃一般食物、能量核心、撞牆／死亡、重新開始與離開遊戲流程。
- [ ] 從遊戲返回主頁後，原本的主題、語言、捲動與導覽行為正常。

## 5. 聯絡表單

- [x] 表單已有前端驗證與測試：必填、姓名長度、Email 格式、訊息長度與 `mailto:` 編碼皆有覆蓋。
- [ ] 人工測試空白、過短／過長姓名、錯誤 Email、過短／過長訊息時，錯誤訊息清楚且與欄位關聯。
- [ ] 用中英文、空白與特殊字元測試合法訊息，確認產生的收件人、主旨與信件內容正確。
- [ ] 在至少一個已設定郵件程式的桌面瀏覽器及手機測試 `mailto:` 開啟行為。
- [ ] 確認介面未聲稱「已送出」；目前設計應明確表示正在開啟使用者的郵件程式。
- [ ] 檢查聯絡信箱是否願意公開，並確認收件匣有垃圾信過濾與備份策略。

## 6. 贊助與綠界（ECPay）金流

### 前端

- [ ] 確認贊助區的中文／英文說明、最低金額 NT$10 與贊助用途符合實際規則。
- [ ] 測試 NT$50、NT$100、NT$300 與自訂金額；自訂金額只允許安全整數且不得低於 NT$10。
- [ ] 測試空白、負數、小數、超大金額與非數字輸入，確認前端會阻擋並顯示適當說明。
- [ ] 未設定 `VITE_PAYMENT_API_ORIGIN` 時，確認網站不提交表單、只顯示「尚在設定」訊息。
- [ ] 已設定 API 網址時，確認送出表單會導向 `<API_ORIGIN>/api/create-payment`。
- [ ] 從付款結果返回首頁後，確認 `?donation=success` 與 `?donation=failed` 的提示正確且可理解。

### 後端與機密設定

- [x] API 已限制付款建立、付款結果與回呼端點只接受 `POST`。
- [x] API 已驗證最小金額 NT$10、最大金額 NT$1,000,000，並產生綠界所需 CheckMacValue。
- [x] 回呼與結果頁會驗證 Merchant ID、訂單編號格式與 CheckMacValue；合法通知回覆 `1|OK`。
- [x] `.env.example` 只提供變數名稱，未包含綠界金鑰或憑證值。
- [ ] 在本機建立不提交 Git 的 `.env`，設定 `SITE_ORIGIN`、`API_ORIGIN`、`ECPAY_ENV`、Merchant ID、Hash Key 與 Hash IV。
- [ ] 確認 `.env` 與任何真實密鑰沒有被 Git 追蹤、打包至前端或寫入公開日誌。
- [ ] 在 Vercel 專案設定相同的伺服器端環境變數；`VITE_PAYMENT_API_ORIGIN` 僅可填公開 API 網址。
- [ ] 以綠界測試環境完成付款建立、綠界頁面跳轉、ReturnURL 回呼、OrderResultURL 返回與成功／失敗顯示的端對端測試。
- [ ] 檢查 Vercel Function Logs：合法付款通知只有必要的交易識別資訊，無 Hash Key／IV、CheckMacValue 或個資。
- [ ] 決定付款成功後的記錄方式（例如資料庫、Email 通知或第三方後台）；目前程式僅驗證並寫入伺服器日誌，不會保存贊助紀錄。
- [ ] 正式上線前確認綠界商店帳號已通過審核，將 `ECPAY_ENV` 改為 `production`，並以小額真實交易驗證一次。
- [ ] 建立退款、爭議款、對帳及付款失敗的客服處理流程與聯絡方式。

## 7. 響應式設計與無障礙

- [ ] 至少在 320px、375px、768px、1024px、1440px 寬度檢查版面；沒有水平捲軸、文字截斷或重疊。
- [ ] 在 iOS Safari、Android Chrome、桌面 Chrome／Edge／Firefox／Safari（如可用）檢查主要流程。
- [ ] 以鍵盤完整走一遍導覽、主題／語言控制、作品連結、遊戲、贊助與聯絡表單。
- [ ] 所有可見焦點樣式清晰，顏色對比足夠，且圖示按鈕具有可辨識名稱。
- [ ] 用螢幕閱讀器抽查標題階層、導覽地標、表單標籤、錯誤訊息與成功／失敗狀態。
- [ ] 確認所有實質圖片都有合適 `alt` 文字；純裝飾圖片應避免造成重複朗讀。
- [ ] 使用瀏覽器縮放至 200% 與系統較大字級測試，確認核心內容仍可閱讀與操作。

## 8. SEO、社群分享與靜態資源

- [ ] 檢查 `index.html` 的 title、description、canonical、Open Graph 與 Twitter Card 文案符合目前個人品牌與網站語言。
- [ ] 確認 canonical 與 `public/sitemap.xml` 中的網址是正式網址 `https://wei20100818.github.io/self-introduction-website/`；若改網域，兩者必須同步更新。
- [ ] 確認 `public/robots.txt` 的 Sitemap 網址與實際發布網址一致。
- [ ] 將正式網址貼到社群預覽工具，確認 `public/images/og-preview.png`、標題與描述顯示正常。
- [ ] 驗證 favicon、個人照片、作品封面與所有靜態資源的正式 URL 均為 200 回應。
- [ ] 檢查圖片尺寸與檔案大小；必要時壓縮或提供 WebP／AVIF，以改善手機載入速度。
- [ ] 使用 Lighthouse 或 PageSpeed Insights 檢查效能、無障礙、最佳實務與 SEO，記錄改善項目。

## 9. 部署與發布

- [x] 專案已有 GitHub Actions 工作流程 `.github/workflows/deploy.yml`，會在 `main` 分支推送或手動觸發時建置並部署 GitHub Pages。
- [x] 工作流程會以 GitHub Variables 中的 `VITE_PAYMENT_API_ORIGIN` 注入靜態網站建置階段。
- [ ] GitHub Pages 設定的來源為 GitHub Actions，且 repository 與 Pages 網址正確。
- [ ] 在 GitHub Repository Variables 設定正確的 `VITE_PAYMENT_API_ORIGIN`，並確認它不是任何密鑰。
- [ ] 在合併至 `main` 前確認變更、測試結果及部署影響範圍。
- [ ] 推送後確認 GitHub Actions 的 deploy workflow 綠燈，並開啟輸出的 Pages URL 驗收。
- [ ] Vercel 專案已連接正確 Git repository／分支，三個 API 路由皆已部署並可達。
- [ ] 確認 GitHub Pages（靜態前端）與 Vercel（付款 API）各自使用 HTTPS，且跨網域表單提交可正常運作。
- [ ] 發布後再次檢查首頁、作品外連、貪食蛇、聯絡表單與付款成功／失敗回傳。
- [ ] 記錄發布版本、日期、網址與已知限制，方便後續回溯。

## 10. 維護與安全

- [ ] 每月或每次重大發布前執行 `npm outdated` 與相依套件安全檢查，評估更新 React、Vite、Vitest、ESLint 與 Actions 版本。
- [ ] 對所有外部連結、聯絡方式、作品截圖與個人介紹建立至少每季一次的更新檢查。
- [ ] 檢查 Vercel／GitHub 的部署紀錄與錯誤日誌，為異常建立通知或固定巡檢。
- [ ] 定期輪替付款金鑰；金鑰疑似外洩時立即在綠界後台撤銷並更新 Vercel 環境變數。
- [ ] 備份本專案原始碼、圖像素材、網域／平台設定與付款帳務資料。
- [ ] 若收集、保存或分析使用者資料，補上隱私權說明與必要的同意機制；目前聯絡表單採使用者本機 `mailto:`，不會由本網站儲存訊息。

## 本次驗證紀錄

| 日期 | 項目 | 結果 |
| --- | --- | --- |
| 2026-08-02 | `npm run lint` | 通過 |
| 2026-08-02 | `npm run test` | 通過（7 個測試檔／17 項測試） |
| 2026-08-02 | `npm run build` | 通過 |

